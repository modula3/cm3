
import ("libm3")
import ("patternmatching")
import ("parseparams")
interface ("regex")

if equal(TARGET, "FreeBSD3") or
   equal(TARGET, "FreeBSD4") or
   equal(TARGET, "FreeBSD5")
  import_lib("compat", "/usr/lib")
end

implementation("Main")
program ("test-regex")

% FIXME: This does not work without some input...
%if defined("RUN")
%  r = q_exec( "." & SL & "test-regex" )
%  write( CR, "program returned ", r, CR )
%end
