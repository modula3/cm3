%
%  Quake functions to instantiate operations on generic polynomials.
%

readonly proc PolynomialBasic (nm, field) is
  local basic  = nm & "PolynomialBasic"
  local vfield = nm & "VectorBasic"
  build_generic_intf (basic, "PolynomialBasic", [field, vfield], VISIBLE)
  build_generic_impl (basic, "PolynomialBasic", [field])
end

readonly proc PolynomialFast (nm, field) is
  local basic  = nm & "PolynomialBasic"
  local fast   = nm & "PolynomialFast"
  local vfield = nm & "VectorFast"
  build_generic_intf (basic, "PolynomialBasic", [field, vfield], VISIBLE)
  build_generic_impl (basic, "PolynomialBasic", [field])
  build_generic_intf (fast,  "PolynomialFast",  [field, vfield, basic], VISIBLE)
  build_generic_impl (fast,  "PolynomialFast",  [field])
end

readonly proc PolynomialFmtLex (nm, pimpl) is
  local poly  = nm & "Polynomial" & pimpl
  local pfmt  = nm & "PolynomialFmtLex"
  local rfmt  = nm & "FmtLex"
  build_generic_intf (pfmt, "PolynomialFmtLex", [rfmt, poly], VISIBLE)
  build_generic_impl (pfmt, "PolynomialFmtLex", [rfmt      ])
end
