/* $Id$ */

if not equal (OS_TYPE, "WIN32")

import ("libm3")
import ("cit_util")

if defined("PTHREADS")
  interface ("SemaphoreC")
  c_source ("SemaphoreC")

  implementation ("Semaphore")
else
  implementation ("SemaphoreUser")
end
interface ("Semaphore")

library ("semaphore")

end
