INTERFACE Space;

(* A Space.T is of a special data type used to record space/occupiedness *)
(* of various planes *)
(* A Space.T is accessed via the Filled interface. *)

TYPE
  T <: REFANY;

END Space.
