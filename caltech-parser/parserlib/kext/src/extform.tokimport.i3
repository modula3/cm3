
  (* Make this interface as good as any token interface *)
  Token = %tok.Token;
  ConstToken = %tok.ConstToken;
  ParseType = %tok.ParseType;
  Allocator = %tok.Allocator;
  Lexer = %tok.Lexer;
  RdLexer = %tok.RdLexer;
CONST
  NewPT = %tok.NewPT;
  Purge = %tok.Purge;
  NewConstToken = %tok.NewConstToken;
