# The shell may not be in /bin. 
#SHELL = sh

#
# Mayank
# For some security reason Interix and SUA both disable execution
# of shell scripts with #!sh in them. Scripts with full path to 
# Shell Interpretor (#!/bin/sh)are only allowed to execute. Hence commenting
# this. Also adding -D_ALL_SOURCE to various CFLAGS 
#

CFLAGS_FOR_BUILD = -g -O2 -D_ALL_SOURCE
CFLAGS = -g -O2 -D_ALL_SOURCE
BOOT_CFLAGS = -g -O2 -D_ALL_SOURCE

# We also need to override LIBGCC2_DEBUG_CFLAGS so libgcc2 will be
# built without debugging information

LIBGCC2_DEBUG_CFLAGS=
