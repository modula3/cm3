readonly TARGET = "SPARC64_LINUX"
readonly WORD_SIZE = "64BITS"
readonly GNU_PLATFORM = "sparc64-linux"

%
% add s switch to ar and remove need for ranlib
%
readonly AR_AND_RANLIB_ARE_ONE = TRUE

%
% fPIC not working?
%
 readonly m3back_flags = "-quiet -gstabs+ -m64"
%readonly m3back_flags = "-quiet -gstabs+ -m64 -fPIC"
 readonly SYSTEM_CC = "gcc -gstabs+ -m64"
%readonly SYSTEM_CC = "gcc -gstabs+ -m64 -fPIC"

readonly SYSTEM_ASM = "as -64"

%
% newer platforms never needed these workarounds for
% bootstrapping with older builds
%
readonly proc GetGcWrapFlags() is return "" end
readonly proc FixM3GcDefs(x) is return x end
readonly proc GetM3BackFlag(x) is return x end

include("Unix.common")
