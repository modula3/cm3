readonly TARGET = "SPARC32_LINUX" % code generation target
readonly WORD_SIZE = "32BITS" % { "32BITS" or "64BITS" }

%
% add s switch to ar and remove need for ranlib
%
readonly AR_AND_RANLIB_ARE_ONE = TRUE

% -fPIC leads to a crash I haven't been able to debug yet

m3back_flags = "-gstabs+ -m32 -munaligned-doubles -funwind-tables"
SYSTEM_CC = "gcc -gstabs+ -m32 -munaligned-doubles" % C compiler
readonly SYSTEM_ASM = "as -32" % Assembler

include("Linux.common")
