readonly TARGET_ARCH = "SPARC"
readonly TARGET_ENDIAN = "BIG"      % { "BIG" OR "LITTLE" }
readonly WORD_SIZE    = "32BITS"    % { "32BITS" or "64BITS" }
m3back_flags = "-munaligned-doubles -funwind-tables -fPIC -m32 -mcpu=v9"
% -munaligned-doubles ?
% -mcpu=v9 for atomics
% -funwind-tables in anticipation of using libunwind
