% Copyright 1996-2000 Critical Mass, Inc. All rights reserved.
% See file COPYRIGHT-CMASS for details.
%

readonly TARGET       = "ARM_DARWIN"
readonly GNU_PLATFORM = "arm-darwin8" % "cpu-os" string for GNU
readonly TARGET_ARCH = "ARM"
readonly TARGET_ENDIAN = "LITTLE"   % { "BIG" OR "LITTLE" }
readonly WORD_SIZE    = "32BITS"    % { "32BITS" or "64BITS" }
readonly GNU_PLATFORM = "arm-apple-darwin8"

readonly SYSTEM_ASM = GNU_PLATFORM & "-as -arch armv6"
SYSTEM_CC = GNU_PLATFORM & "-gcc -fPIC -march=armv6 -mcpu=arm1176jzf-s"
readonly SYSTEM_LIBTOOL = GNU_PLATFORM & "-libtool"

m3back_flags = "-fPIC -gstabs+ -march=armv6 -mcpu=arm1176jzf-s -fverbose-asm"
% -funwind-tables?
% -m32?

include("Darwin.common")
